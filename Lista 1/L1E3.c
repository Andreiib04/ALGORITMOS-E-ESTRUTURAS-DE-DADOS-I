#include <stdio.h>
#include <stdlib.h>

int main(){
    int numero, soma;
    printf("Digite o valor :\t");
    scanf("%d", &numero);
    
 soma= numero*numero;
 printf("A soma dos valores é igual a %d", soma);


    return 0;
}